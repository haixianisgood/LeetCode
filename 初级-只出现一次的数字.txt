给定一个非空整数数组，除了某个元素只出现一次以外，其余每个元素均出现两次。找出那个只出现了一次的元素。

说明：

你的算法应该具有线性时间复杂度。 你可以不使用额外空间来实现吗？

示例 1:

输入: [2,2,1]
输出: 1

示例 2:

输入: [4,1,2,1,2]
输出: 4

解法一：用额外空间，利用map进行计数
解法二：若用暴力法求解，每一个数都在数组中寻找其相同的数，则为O(N²)
解法三：异或，把所有数进行异或，结果即为不重复的数，因为两个数字相同，异或肯定为0，即最后结果为唯一数与0进行异或，最终结果等于该唯一数
