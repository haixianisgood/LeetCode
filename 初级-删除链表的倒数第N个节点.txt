给定一个链表，删除链表的倒数第 n 个节点，并且返回链表的头结点。

示例：

给定一个链表: 1->2->3->4->5, 和 n = 2.

当删除了倒数第二个节点后，链表变为 1->2->3->5.
说明：

给定的 n 保证是有效的。

进阶：

你能尝试使用一趟扫描实现吗？

解法一：先遍历一次，求出size，然后再把倒数n+1的结点指向倒数n-1的结点。
解法二：建立一个变量存储第n-1个结点，然后进行迭代，每次更新第n-1结点，到了第n个就删除即可。注意链表的长度，不要越界。
